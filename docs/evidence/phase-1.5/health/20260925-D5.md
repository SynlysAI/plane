# 2026-09-25 Phase 1.5 D5 收敛与移交复演

> **历史证据**：本文记录 2026-09-25 Phase 1.5 旧 seed 联调结果，只用于审计复盘，不代表当前 `public` π-Lab Excel 基线。当前人工测试请使用 Phase 1.5 分角色人工测试计划。

## 最终测试结果

| 范围                             | 结果                  |
| -------------------------------- | --------------------- |
| Plane Research 相关后端单元/契约 | 51 passed             |
| Synlora Research 契约            | 19 passed / 6 skipped |
| RAGPortal 全量后端               | 33 passed / 1 warning |
| Plane Web 组件                   | 85 passed             |

## 五服务最终健康快照

| 服务      | 状态                                | 版本 / 提交        |
| --------- | ----------------------------------- | ------------------ |
| Plane Web | HTTP 200                            | 4.13.4 / f08a92898 |
| Plane API | Research health 200，module enabled | 4.13.4 / f08a92898 |
| Synlora   | `/api/health` 200                   | 1.4.1 / 80f854e    |
| RAGPortal | `/api/health` 200                   | 0.1.4 / 5efbe91    |
| WeKnora   | `/` HTTP 200                        | 已部署实例         |

Plane、Synlora、RAGPortal 的 develop 分支均与远端对齐，工作区无未提交变更。

## 观测指标

- RAGPortal integration.call：SUCCESS 76 次，平均 70.5 ms；DEGRADED 3 次；FAILED 6 次（均为演练或修复前预期样本）。
- Agent run events：129 条，`(run_id, seq)` 唯一键 129 条，重复 0；单轮最大 seq 114。
- Research Chain events：按 `(node_id, event_id)` 聚合的重复数为 0。
- public 工作区 module / chain / agent / external RAG / AccountLink / IA v2 六个开关均开启。
- `liuyang.phd` 的 Synlora AccountLink 为 ACTIVE，external subject `u_liuyang_phase15`。

## Phase 1 灰度手册复演

### 准备外部服务

- WeKnora 内网实例可达；RAGPortal 与 Synlora 均按单实例启动并通过健康检查。
- Plane 容器可经 Docker 网关访问 8004/8005；RAGPortal 集成连接启用，BFF KB list 返回 5 个知识库。
- Synlora delegated token、capability manifest、`agent-context.v2`、WeKnora hybrid search 均在真实服务间通过。

### 内部试点

- 试点 public 工作区开启 Phase 1.5 所需 research 子开关。
- 试点学生 AccountLink ACTIVE。
- 已完成双课题创建、WORKSPACE/PRIVATE ACL、PDF 入库引用、Agent `knowledge.search`、事件回放、typed snapshot 与 Markdown 导出。
- 降级三项均验证“人工记录不阻断”，恢复后 Chain / Agent / 上传链路全部回到绿色。

## 缺陷收敛

P15-001 至 P15-008 全部关闭，均记录根因、修复提交与回归证据。其中 P0/P1 共 7 条，P2 1 条；无遗留 P0/P1。
