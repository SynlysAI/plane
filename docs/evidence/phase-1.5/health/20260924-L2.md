# 2026-09-24 Phase 1.5 L2 单服务契约回归

> **历史证据**：本文记录 2026-09-25 Phase 1.5 旧 seed 联调结果，只用于审计复盘，不代表当前 `public` π-Lab Excel 基线。当前人工测试请使用 Phase 1.5 分角色人工测试计划。

| 用例                            | 执行目录                                | 结果                 |
| ------------------------------- | --------------------------------------- | -------------------- |
| L2-1 Plane API contract         | `docker-compose-test.yml` / `api-tests` | 3 passed             |
| L2-2 Synlora research contracts | `Synlora/apps/web/backend`              | 19 passed, 6 skipped |
| L2-3 RAGPortal full pytest      | `RAGPortal/backend`                     | 31 passed, 1 warning |

## 命令

```bash
docker compose -f docker-compose-test.yml run --rm api-tests \
  pytest -q plane/tests/contract/app/test_research_phase1_e2e.py \
  plane/tests/contract/app/test_research_agent_orchestrator.py

cd ../Synlora/apps/web/backend
.venv/bin/pytest -q \
  tests/test_research_context.py \
  tests/test_research_agent_contracts.py \
  tests/test_runtime_assembly.py \
  tests/test_session_runtime.py

cd ../../RAGPortal/backend
set -a; source ../.env; set +a
.venv/bin/pytest -q
```

RAGPortal 的 warning 来自 `pytest-asyncio` 配置项缺省与 Starlette testclient 的第三方弃用提示，不影响契约结果。
