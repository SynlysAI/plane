# Phase 1.5 分角色验证证据目录

本目录保存当前 `public` π-Lab 基线上的分角色人工测试证据。证据文件只允许包含脱敏角色标识、对象 ID（可截断）、状态码、错误码、request id、结论和清理状态。

禁止提交密码、Token、API Key、完整邮箱、原始课题正文、原始 Excel、未脱敏截图和运行时日志。启动健康快照放在上级 `health/` 目录；历史 seed 证据放在 `issue0925/` 等历史目录，不与当前角色验证混用。
