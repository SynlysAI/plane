# 2026-09-25 Phase 1.5 降级演练

> **历史证据**：本文记录 2026-09-25 Phase 1.5 旧 seed 联调结果，只用于审计复盘，不代表当前 `public` π-Lab Excel 基线。当前人工测试请使用 Phase 1.5 分角色人工测试计划。

## DEG-1 停止 Synlora

### 演练

- 停止 tmux 单实例 `phase15_syn`，8005 健康检查失败。
- 新 Agent session 返回 HTTP 503，`error_code=AGENT_UPSTREAM_NOT_CONFIGURED`。
- 同一节点手工 Chain event 返回 HTTP 201，request id `phase15-deg1-manual-93f6fd53e1194eaeaf4537cdb3d2f07e`。
- Agent 页面显示：“智能体运行时尚未配置，研究链功能仍可继续使用。”截图：`20260925-DEG-1.png`。

### 恢复

- Synlora 1.4.1 重启后健康检查通过。
- 新 Agent session `e4fbfa9c-5782-40ec-afd3-44dd0cd8d63f` 创建 HTTP 201，Synlora session `4c2377c42680`。
- 旧 run `after_seq=113` 仅返回 seq 114；114 个事件的重复数为 0。

## DEG-2 停止 RAGPortal

### 演练

- 停止 tmux 单实例 `phase15_rag`，8004 健康检查失败。
- BFF 上传返回 HTTP 503，`degraded_reason=transport_error`，`manual_record=true`，Plane 记录 DEGRADED。
- 同一节点手工 Chain event HTTP 201。
- 页面显示：“RAGPortal 当前降级（网络传输失败），人工记录可继续。”截图：`20260925-DEG-2.png`。

### 恢复

- RAGPortal 0.1.4 重启后健康检查通过。
- BFF 知识库列表返回 200、5 个 KB、非降级；既有 PDF 记录保持 SUCCESS。

## DEG-3 WeKnora key 失效

### 演练

- 保存原 key 后将 `WEKNORA_API_KEY` 替换为无效值并重启 RAGPortal。
- 修复前：知识库列表 / 上传抛出 WeknoraError，RAGPortal 返回 500，Plane 降级 `http_error`。
- RAGPortal 修复提交 `5efbe91` 后：
  - `/api/kb/list` 返回 401 与中文 `WeKnora 认证失败，请检查 API Key`。
  - Plane BFF 上传返回 HTTP 503，`degraded_reason=unauthorized`，`manual_record=true`。
  - 同一节点手工 Chain event HTTP 201。
  - 页面显示：“RAGPortal 当前降级（认证失败），人工记录可继续。”截图：`20260925-DEG-3.png`。

### 恢复

- 立即恢复原 key 并重启 RAGPortal。
- WeKnora `/` 返回 HTTP 200；BFF 知识库列表返回 200、5 个 KB。
- 失败文件 `deg3-fixed.txt` 使用新 request id 重传，复用同 hash 记录 `b6a93aa1-3139-4c07-826f-91e8f01fff42`，返回 knowledge/task `2cddc6a8-c477-42f9-a67a-4f59015c60b2`。
- 状态轮询 `PROCESSING → SUCCESS`；同 hash 记录数为 1。
